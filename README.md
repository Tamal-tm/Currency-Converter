# Currency-Converter

Project Overview

This is a simple Python program that converts one currency to another using fixed (predefined) exchange rates.
It helps users understand how if–else statements and basic arithmetic can be used to build small, functional projects.

Features

Converts between common currencies like USD, EUR, INR.

Uses simple if–else logic (no APIs or complex libraries)

Takes input directly from the user

Displays the converted amount clearly
